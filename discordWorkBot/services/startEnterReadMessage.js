function startEnterReadMessage() {
    let reply = "Enter reads (readtype..readname..attempts..complete..postcodes)"
    let recordMessages = true;
    console.log("started");
    return {
        reply,
        recordMessages
    }
}